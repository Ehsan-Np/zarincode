{
  "name": "zarincode-theme",
  "private": true,
  "version": "3.38.0",
  "scripts": {
    "lint:js": "node --check assets/js/main.js && node --check assets/js/panel.js && node --check assets/js/admin.js && node --check assets/js/jalali.js && node --check assets/js/player.js && node --check assets/js/dark.js",
    "test": "npm run lint:js",
    "env:start": "wp-env start --update",
    "env:stop": "wp-env stop",
    "env:clean": "wp-env clean all"
  },
  "devDependencies": {
    "@wordpress/env": "^10.27.0"
  }
}
